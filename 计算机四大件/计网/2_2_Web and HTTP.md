# <center><font face="宋体" color=orange>2.2 Web and HTTP</font></center>   
## Web
**Web页**：由一些对象(HTML文件，JPEG图像，Java小程序等)组成，Web页   
Web页含有一个基本的HTML文件，该基本HTML文件又包含若干对象的引用(链接)，通过URL(访问协议，用户名，口令字，端口)`(Prot://user:psw@ysu.edu/someDept/pic.gif:port)`对每个对象进行引用
## HTTP(Web的应用层协议)
HTTP是无状态的    
HTTP按是否持久分为持久HTTP连接(是否流水方式)和非持久HTTP    

![HTTP连接](https://vip4.yunpng.top/2025/11/11/6912d5dc372db.png)    


**扩展**   
* 用户-服务器状态：cookies(4组成)
* Web缓存(代理服务器)